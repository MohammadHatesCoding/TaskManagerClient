export interface User {
    id : string,
    name: string;
    lastName: string;
    username: string;
    email: string;
    phoneNumber: string;
    birthDate: string; // string in frontend (ISO date)
}